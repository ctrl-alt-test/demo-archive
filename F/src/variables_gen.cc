// Ce fichier sera généré
#include "variables_gen.hh"

namespace Variables
{
  int fade_times[] = { 1, 2, 3, 4 };
  float fade_data[] = { 0.f, 0.5f, 0.f, 1.f };
  Variable fade(0, 0, 0, 0);

  void init()
  {
    DECL(fade);
  }
}
