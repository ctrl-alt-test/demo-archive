//
// Définition des temps absolus en ms pour le déroulement
//

#include "timing.hh"

const date worldStartDate = 0;
const date worldEndDate = worldStartDate + ((3*60 + 21) * 1000);

const date demoEndDate = worldEndDate;
