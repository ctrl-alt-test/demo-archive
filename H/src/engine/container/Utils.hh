#ifndef CONTAINER_UTILS_HH
#define CONTAINER_UTILS_HH

/// <summary>
/// Number of elements of a statically declared const array.
/// </summary>
#define ARRAY_LEN(array) (sizeof(array) / sizeof(array[0]))

#endif // CONTAINER_UTILS_HH
